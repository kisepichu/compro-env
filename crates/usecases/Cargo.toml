[package]
name = "usecases"
version = "0.1.0"
edition = "2024"

[dependencies]
domain = { workspace = true }
anyhow = { workspace = true }
chrono = { workspace = true }
toml = "0.8"

[dev-dependencies]
mockall = "0.13"
tempfile = "3"
