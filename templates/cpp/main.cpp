#include <bits/stdc++.h>
using namespace std;

void solve() {
    // read input with cin, write output with cout
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    // cin >> t; // uncomment for multiple test cases
    while (t--) solve();
    return 0;
}
